id = "undo-last-commit"
namespace = "git"
title = "Undo the last commit but keep changes staged"
description = "Removes the last commit from history while keeping the file changes staged and ready to re-commit."
example = "git reset --soft HEAD~1"
command = "git reset --soft HEAD~1"
tags = ["git", "undo", "commit", "reset"]
danger = "low"
args = []
