id = "list-security-groups"
namespace = "aws"
title = "List security groups"
description = "Lists EC2 security groups."
example = "aws ec2 describe-security-groups --profile production"
command = "aws ec2 describe-security-groups --profile {profile}"
tags = ["aws", "ec2", "security-groups", "network"]
danger = "low"
args = [
  { name = "profile", prompt = "AWS profile to use", default = "default" }
]
